Immediate on/off setting (applies without a save step).

```jsx
<Switch checked={enabled} onChange={setEnabled} label="Cho phép giao dịch trực tuyến" />
```

- Use for instant-effect settings. For form fields that submit later, use `Checkbox`.
