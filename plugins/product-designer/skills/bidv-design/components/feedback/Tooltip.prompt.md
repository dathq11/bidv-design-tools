Hover hint for icon-only controls and truncated text.

```jsx
<Tooltip label="Xuất Excel"><IconButton icon="download" ariaLabel="Xuất Excel" /></Tooltip>
```

- Dark bubble (`--gray-900`), 12px text. Keep labels to a few words.
- Use on every icon-only button whose meaning isn't obvious.
